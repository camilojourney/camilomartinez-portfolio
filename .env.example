# Environment Variables Configuration
#
# 1. OpenAI Integration
# Get your API key from: https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key_here

# 2. WHOOP API Integration
# Get your WHOOP credentials from: https://developer.whoop.com/
# Create a new app with these redirect URLs:
#    - http://localhost:3005/api/auth/callback/whoop (for development)
#    - https://yourdomain.com/api/auth/callback/whoop (for production)
# Copy your Client ID and Client Secret

# Add these to your .env.local file (create if it doesn't exist):
WHOOP_CLIENT_ID=REPLACE_ME_here
WHOOP_CLIENT_SECRET=REPLACE_ME_here
AUTH_SECRET=your_random_32_character_secret_here

# 3. Strava API Integration (for Astoria Conquest)
# Get your Strava credentials from: https://developers.strava.com/
# Create a new app with these settings:
#    - Application Name: Your App Name
#    - Category: Training
#    - Website: https://yourdomain.com
#    - Authorization Callback Domain: yourdomain.com
# Copy your Client ID and Client Secret
STRAVA_CLIENT_ID=REPLACE_ME_here
STRAVA_CLIENT_SECRET=REPLACE_ME_here
STRAVA_REFRESH_TOKEN=your_personal_refresh_token_for_cron_jobs

# 4. Database Configuration (Vercel Postgres)
# Get these from your Vercel dashboard > Storage > Postgres
POSTGRES_URL=postgresql://username:password@hostname/database
POSTGRES_URL_NON_POOLING=postgresql://username:password@hostname/database

# 5. NextAuth Configuration
NEXTAUTH_URL=http://localhost:3005  # Change to your domain in production
NEXTAUTH_SECRET=your_random_32_character_secret_here

# 6. Cron Job Security
# Generate a random secret for securing cron endpoints
CRON_SECRET=your_random_cron_secret_here

# 7. Admin Access Controls
# Comma-separated allowlists used by protected internal endpoints
ADMIN_EMAILS=you@example.com
ADMIN_USER_IDS=12345
INTERNAL_API_SECRET=your_internal_api_secret_here

# 8. Public Dashboard Data Exposure
# Set to true only if you intentionally want /api/view-data accessible without auth
ALLOW_PUBLIC_DASHBOARD_DATA=false

# 9. Astoria Conquest Configuration (optional)
# Define the boundary of Astoria for geospatial queries
NEXT_PUBLIC_ASTORIA_BOUNDARY='[[[lng,lat],[lng,lat],...]]'  # GeoJSON polygon coordinates

# Example AUTH_SECRET generation:
# You can use this online: https://generate-secret.vercel.app/
# Or run: openssl rand -base64 32

# For production (Vercel), add these same variables to:
# Vercel Dashboard > Project > Settings > Environment Variables
