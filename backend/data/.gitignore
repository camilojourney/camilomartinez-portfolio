# Astoria Conquest cache files (generated)
# Note: astoria_graph.pkl is committed so Render has it on deploy
astoria-conquest/cache/*.geojson

# OSMnx HTTP cache (generated)
osmnx-cache/*.json

# Keep directory structure
!.gitkeep
