import { NextResponse } from 'next/server';

export async function GET() {
    return NextResponse.json({ message: 'Please use /api/whoop-collector-v2 instead' });
}

export async function POST() {
    return NextResponse.json({ message: 'Please use /api/whoop-collector-v2 instead' });
}
